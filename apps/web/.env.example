# ============================================
# API Configuration
# ============================================
# Backend API URL that the frontend will connect to
# 
# Development: http://localhost:3000 (default NestJS port)
# Production: Your actual backend API URL
#   - If using same domain: https://yourdomain.com/api
#   - If using subdomain: https://api.yourdomain.com
#   - If using EC2 directly: http://your-ec2-ip:3000
#
# IMPORTANT: 
# - No trailing slash
# - Must be accessible from the browser (not internal Docker network)
# - In production, this value is set during Docker build (see Dockerfile)
VITE_API_URL=http://localhost:3000

